# Kit events feed. Get these from Kit: Admin → Events calendar & feed.
# In Netlify these are set as build environment variables, not committed.
KIT_EVENTS_URL=https://kit.twdata.org/gravity-brewing/events/feed.json
KIT_EVENTS_TOKEN=
