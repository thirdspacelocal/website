# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# Local capture (reference only — not part of the site)
/_capture/

# OS / editor
.DS_Store

# Local toolchain + secrets (see Makefile / .env.example)
/.bin/
.env
